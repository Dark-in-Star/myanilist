# Add project-specific ProGuard rules here if minifyEnabled is ever turned on.
# The app currently ships almost no Java/Kotlin code of its own (just androidbrowserhelper's
# LauncherActivity), so shrinking isn't enabled by default — see app/build.gradle.
